const nav=document.querySelector('.nav'),menu=document.querySelector('.menu'),links=document.querySelector('.nav-links');
menu?.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',open);menu.textContent=open?'×':'☰'});
links?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));
addEventListener('scroll',()=>nav?.classList.toggle('scrolled',scrollY>30));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const form=document.querySelector('#contactForm');
if(form){const fields=[...form.querySelectorAll('[required]')],status=document.querySelector('.form-status');const check=f=>{const wrap=f.closest('.field'),bad=!f.checkValidity();wrap.classList.toggle('error',bad);wrap.querySelector('.error-msg').textContent=f.validity.typeMismatch?'Introduceți o adresă de e-mail validă.':'Acest câmp este obligatoriu.';return !bad};fields.forEach(f=>{f.addEventListener('blur',()=>check(f));f.addEventListener('input',()=>{if(f.closest('.field').classList.contains('error'))check(f)})});form.addEventListener('submit',e=>{e.preventDefault();status.classList.remove('show');const valid=fields.map(check).every(Boolean);if(!valid){form.querySelector('.error input,.error textarea')?.focus();return}status.classList.add('show');status.innerHTML='<strong>Mesaj trimis.</strong><br>Mulțumim — revenim cu un răspuns în cel mult o zi lucrătoare.';form.reset();fields.forEach(f=>f.closest('.field').classList.remove('error'));status.scrollIntoView({behavior:'smooth',block:'nearest'})})}
